HOSTS:
  debian8:
    roles:
      - master
      - debian8
    platform: debian-8-amd64
    image: debian:8
    hypervisor: docker
    docker_image_commands:
      - 'apt-get install -y sudo ufw wget'
    docker_cmd: '["/sbin/init"]'
CONFIG: 
  type: foss
  log_level: debug
